class TeeBox {

    int id;
    int courseId;
    String name;
    int slope;
    double rating;
    int teeBoxType;

    TeeBox(this.id, this.courseId, this.name, this.slope, this.rating, this.teeBoxType);
}